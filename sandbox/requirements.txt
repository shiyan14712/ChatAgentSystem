# Agent Sandbox — pre-installed Python packages
# These are baked into the Docker image so they are instantly available
# at execution time without a pip install step.

# Scientific computing
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.12.0
sympy>=1.13.0

# Visualization
matplotlib>=3.8.0

# Data utilities
requests>=2.31.0
python-dateutil>=2.9.0

# Text / JSON
tabulate>=0.9.0
pyyaml>=6.0.0
